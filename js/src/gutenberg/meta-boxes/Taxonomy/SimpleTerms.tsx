import type {FromPanel} from './WithTaxonomyPanel';

import styles from './simple-terms.pcss';


const SimpleTerms = ( {}: FromPanel ) => {
	return (
		<div className={styles.wrap}>
		</div>
	);
};

export default SimpleTerms;
